# Phase 9D — Docling Semantic Canary

This phase introduces a local-only, schema-conditioned Docling extraction canary behind the existing semantic candidate and evidence-grounding contracts. It must not write directly to canonical business truth.
